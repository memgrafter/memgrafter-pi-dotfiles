# extensions/draft/

In-progress extensions not yet added to settings.json.

- `pi-claude-cache-warming.ts` → keep Claude OAuth session cache warm after idle periods
- `pi-tree-prewarm.ts` → add cache breakpoint on tree target before /tree executes
- `pi-dynamic-tools.ts` → load tool definitions into context on demand
