# memgrafter-pi-dotfiles

Skeleton for a local **pi package**.

## Layout

- `extensions/` → TypeScript/JavaScript extensions (`*.ts`, `*.js`)
- `skills/` → skills (`SKILL.md` folders or top-level `*.md`)
- `prompts/` → prompt templates (`*.md`)
- `themes/` → themes (`*.json`)
