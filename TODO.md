# TODO

- [ ] `pi-codex-rotator`: status/rotation output color still appears yellow in the current theme. Investigate why custom audit renderer (`pi-codex-rotator-audit`) is not taking effect in the user-visible output path, then force normal theme text color for:
  - `/codex-rotator status` output
  - rotation notice lines (e.g., `pi-codex-rotator: rotated openai-codex ...`)
